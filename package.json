{
  "name": "desktop-toolkit-monorepo",
  "private": true,
  "version": "1.0.1",
  "description": "Monorepo root for desktop-toolkit — see js/packages/desktop-toolkit for the published JS package and python/ for the Python package.",
  "license": "MIT",
  "workspaces": [
    "js/packages/*"
  ],
  "scripts": {
    "lint:css": "npm run lint:css --workspace @chamber-19/desktop-toolkit",
    "lint:css:fix": "npm run lint:css:fix --workspace @chamber-19/desktop-toolkit"
  },
  "engines": {
    "node": ">=20"
  }
}
